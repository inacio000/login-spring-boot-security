import styles from './style.css'

export function SubscribeButton() {
    return (
        <button
            type='button'
            className="subscribeButton"
        >
            Subscribe now
        </button>
    )
}